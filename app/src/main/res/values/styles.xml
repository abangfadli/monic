<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
    </style>


    <style name="BaseText">

    </style>

    <style name="SectionTitle" parent="BaseText">
        <item name="android:textSize">@dimen/section_title</item>
        <item name="android:textColor">@color/section_title</item>
        <item name="android:textStyle">bold</item>
    </style>


    <style name="SearchResultButton">
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_filter_button</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_size_xlarge_16</item>
    </style>
</resources>
